# Journal — 2026-08-25/26 Deploy: Sophia 2027 Slice 1 (docs-only)

Commit: 33d8b82d5 · Deployed: 2026-08-25T17:52:10Z · Agent: git-manager (P5)

## Action
Shipped docs-only slice (recon refresh 2026-08-25 + constitution v1.1 + app mirrors) per CF-direct doctrine to preserve main==prod-SHA invariant.

## Decision
1. First `deploy:full` attempt (commit 9f15e4ac) aborted: script's raw `npm test` gate fails on pinned known-red C1 (`land/youtube actions.test.ts:322`). Per sophia-deploy-verify.md, deploying on known-broken base requires WARNING line in commit message → amended commit to 33d8b82d5 with WARNING, force-push-with-lease, re-ran with `SKIP_TESTS=1` (sole bypass; precedent: journal 20260820-getd1-go-live). tsc, signature, and pre-deploy gates stayed enabled.
2. Webhook GET probes returned 503 `canary_window` instead of plan-expected 4xx. Investigated before rollback: structured response originates from `src/middleware-api-handler.ts:19-32` (webhook version-pinning, requires INTERNAL_API_SECRET), introduced 2026-04-25 (455bbd6d1) — ancestor of pre-deploy base. Pre-existing by design; docs-only commit (zero src/ files) cannot change runtime middleware. Classified KNOWN-RED, non-blocking.
3. `/login` returns 307 → `/` → 200 (locale redirect); final destination 200 = healthy.

## Outcome
GREEN. deploy:full exit 0; /api/version shortSha 33d8b82d == local HEAD; deployedAt updated; /api/health 200; /vi/login 200; /vi/setup-wizard 200; zero migrations; post-deploy smoke passed.

## Lessons
- deploy-with-sha.sh test gate has no pinned-known-red tolerance: any deploy on the C1-broken base needs SKIP_TESTS=1 + WARNING commit line. Budget the amend cycle.
- Pipe deploy output through `tail` masks exit codes; capture full log to file and read `$?` directly.
- Plan probe expectations ("IPN returns 4xx") were stale vs the canary-pinning reality — verify mechanism source before treating 5xx as regression.
