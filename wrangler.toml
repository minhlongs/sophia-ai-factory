[build]
command = "pnpm build"
publish = ".next"

# Exclude large cache files
exclude = [
  ".next/cache/**",
  "node_modules/**",
  "*.log"
]

[functions]
directory = ".next/server"

[[redirects]]
  from = "/api/*"
  to = "/api/:splat"
  status = 200
