# 🚀 Agency OS (Lean Architecture) - Dependencies
# ===============================================

# --- Core Framework ---
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# --- HTTP & Networking ---
requests>=2.31.0
python-multipart>=0.0.6  # For form data parsing (webhooks)

# --- Database & Infra ---
supabase>=2.0.0
python-dotenv>=1.0.0

# --- Payment Gateways ---
stripe>=7.0.0
# PayPal SDK is implemented internally using requests

# --- Utilities ---
typing_extensions>=4.0.0
email-validator>=2.0.0

# --- Security ---
# cryptography (often pulled in by others, but good to be explicit if needed for hashing)
