# Changelog — 2026 Q3

---

## [2026-09-08] SUPREME COMMAND #7 — fal.ai Image Adapter (Experimental)

**Summary (vi):** Thêm fal.ai làm provider sinh ảnh thử nghiệm (EXPERIMENTAL) bên cạnh MuAPI hiện tại. Adapter `FalImageProvider` implement `ImageGenerationProvider` interface, dùng circuit breaker, BYOK cho `FAL_KEY`, tier gating (BASIC/PREMIUM = fal-schnell only, ENTERPRISE+/MASTER = all models), lưu kết quả vào `media_jobs` D1 table. MuAPI route không bị ảnh hưởng.

**Summary (en):** Added fal.ai as an experimental image generation provider alongside existing MuAPI. `FalImageProvider` implements `ImageGenerationProvider` interface, uses circuit breaker wrapping, BYOK for `FAL_KEY`, tier gating (BASIC/PREMIUM = fal-schnell only, ENTERPRISE+/MASTER = all models), persists results to `media_jobs` D1 table. MuAPI route untouched.

**Files created:** `seed/ai/providers/fal-image-provider.ts` (189 LOC), `seed/ai/providers/__tests__/fal-image-provider.test.ts` (28 tests), `app/api/v1/creative-studio/images/generate/__tests__/fal-provider-route.test.ts` (8 tests)

**Files modified:** `tree/byok/user-api-key-store.ts` (+'fal-ai' to ByokProvider), `app/actions/image-generate-action.ts` (+fal-ai routing block), `app/api/v1/creative-studio/images/generate/route.ts` (+fal-ai routing), `app/api/v1/creative-studio/images/[id]/status/route.ts` (+provider guard), `app/api/user/byok/test/route.ts` (+fal-ai test endpoint)

**Verification:**
- Type-check: ✅ 0 errors
- Tests: ✅ 8829 passed | 0 failed (36 new tests: 28 unit + 8 integration)
- Lint: ✅ 0 errors in all touched files
- G1-G10 gates: ✅ all pass
- No deploy/push/commit performed — awaiting user approval for Phase 7

**Classification:** EXPERIMENTAL — registered via `registerCertification('fal-ai', { state: 'EXPERIMENTAL' })`

---

## [2026-09-07] SUPREME COMMAND #5 — Provider Certification Enforcement + Real Image Cell Foundation

**Summary (vi):** Triển khai hệ thống chứng nhận provider (5 trạng thái: NOT_CERTIFIED → PRODUCTION_READY / BLOCKED) với gate enforced tại provider-factory, error type PROVIDER_NOT_CERTIFIED tích hợp circuit breaker, và adapter OpenRouterImageGenerationAdapter (ImageGenerationProvider) thay mock trong creative-image-generate.

**Summary (en):** Implemented provider certification system (5-state machine: NOT_CERTIFIED → PRODUCTION_READY / BLOCKED) with enforcement gate in provider-factory, PROVIDER_NOT_CERTIFIED error type integrated into circuit breaker, and OpenRouterImageGenerationAdapter (ImageGenerationProvider) replacing the mock in creative-image-generate.

**Files created:** `seed/ai/provider-certification.ts`, `seed/ai/providers/openrouter-image-generation-adapter.ts`, 2 test files (10 + 10 tests), `docs/PROVIDER_CERTIFICATION_POLICY.md`

**Files modified:** `failure-kind.ts` (+PROVIDER_NOT_CERTIFIED), `circuit-breaker.ts` (+cooldown + immediateOpen), `provider-factory.ts` (+cert gate), `hermes-antigravity-adapter.ts` (+registerCertification BLOCKED + vision fix), `creative-image-generate.ts` (+resolveImageProvider), `CLAUDE.md` (+canonical imports)

**Verification:**
- Type-check: ✅ 0 errors
- Tests: ✅ 8877 passed | 0 failed | 34 skipped | 10 todo (858 total files)
- Lint: ✅ 0 errors in all touched files (11 pre-existing errors in unrelated files)
- No existing tests removed or decreased
- No deploy/push/commit performed

**Classification:** PRODUCTION CANDIDATE — all code-level gates satisfied; actual PRODUCTION READY requires deployed evidence.

---

## [2026-08-26] Sophia 2027 Phase 2 — Creative Intelligence Execution

**Summary (vi):** Thực thi Phase 2 SOPHIA 2027 (Creative Intelligence): (1) cứu lỗi schema — migration 0254 tạo 3 bảng `experiments`/`experiment_variants`/`experiment_results` mà code đã query từ trước nhưng chưa từng tồn tại, (2) nạp tín hiệu thị trường — bảng `market_signals` (0255), store + nguồn YouTube trending BYOK + Google Trends RSS, cron mỗi giờ `market-signals-ingest` đã đăng ký Inngest (34 functions), (3) phát hiện xu hướng đa kênh — `tree/trend-intelligence/` tính velocity + z-score với hệ số mùa vụ/người xem, dự báo SES 7 ngày, lưu `trend_detections` (0256), (4) đọc graph qua API — server actions + `GET /api/graphs/[type]` (ip-lineage, content-lineage, content-performance, projects, ip-list) xác thực 401/403/404, (5) hợp nhất experiment engine — bridge chỉ-đọc từ `ab_experiments` sang domain `Experiment`, learning-loop E2E xanh, KPI 12 experiment đồng thời đạt, (6) mô hình hiệu suất heuristic tất định + CLI backtest offline, (7) adapter sinh ảnh OpenRouter (BYOK).

**Summary (en):** Executed Sophia 2027 Phase 2 (Creative Intelligence): (1) schema rescue — migration 0254 creates the three `experiments`/`experiment_variants`/`experiment_results` tables that repo code had been querying without any backing table; (2) market signal ingestion — `market_signals` table (0255), store + YouTube-trending BYOK source + Google Trends RSS source, hourly cron `market-signals-ingest` registered in the Inngest route (functions array now 34); (3) cross-channel trend intelligence — velocity + z-score detection with seasonal/audience multipliers and 7-day SES forecast persisted to `trend_detections` (0256); (4) read-only graph API — server actions + `GET /api/graphs/[type]` with 401/403/404 gating; (5) experiment engine consolidation — read-only bridge from production `ab_experiments` to the unified `Experiment` domain type, learning-loop end-to-end green, 12-concurrent-experiments KPI pass; (6) deterministic heuristic performance scorer + offline backtest CLI; (7) OpenRouter image-generation adapter (BYOK).

**Migrations:** 0254_experiments_tables.sql, 0255_market_signals.sql, 0256_trend_detections.sql (all additive CREATE TABLE IF NOT EXISTS)

**Verification:**
- Targeted cross-lane tests: ✅ 21 files / 430 tests passed (`vitest run src/tree/market-signals src/tree/trend-intelligence src/tree/performance src/forest/ab src/land/graphs src/seed/ai`)
- Full gates (tsc / lint / test / build): recorded in `.orchestrate/latest/execution-H.md`
- Backtest precision: ⚠️ synthetic fixture only (100% on deterministic data) — **production replay pending; not yet evidence of the >70% KPI**

**Key files:** `tree/market-signals/`, `tree/trend-intelligence/`, `tree/performance/{experiment,scoring}.ts`, `forest/ab/engine-bridge.ts`, `land/graphs/actions.ts`, `app/api/graphs/[type]/route.ts`, `seed/ai/providers/openrouter-image-adapter.ts`, `scripts/backtest-performance-model.ts`, `migrations/025{4,5,6}_*.sql`

---

## [2026-08-26] Sophia 2027 Phase 1 — Domain Primitives Execution

**Summary (vi):** Thực thi Phase 1 SOPHIA 2027: tiêm CreativeIdentity vào agent runs (identity block trong system prompt, backward-compatible khi không có identity), CreativeMemory flywheel (đọc memories trước mỗi run, persist learning sau run thành công — category creative, source agent), đóng vòng lặp approval qua event `agent.approval.resolved` (fire-and-forget non-fatal từ resolveApprovalAction), dedupe MarketSignal về single source trong seed/types/creative-domain.ts, thêm 34 test content-graph (types + integration với D1 shim sqlite thật).

**Summary (en):** Executed Sophia 2027 Phase 1: injected CreativeIdentity into agent runs (system-prompt identity block, backward-compatible when absent), CreativeMemory flywheel (read memories before each run, persist learnings after successful runs — category creative, source agent), closed the approval loop via `agent.approval.resolved` event (fire-and-forget, non-fatal from resolveApprovalAction), deduped MarketSignal to a single declaration in seed/types/creative-domain.ts, added 34 content-graph tests (unit + integration with real node:sqlite D1 shim).

**Commits:** `c18ccb8e4` (dedupe), `dcc25e2af` (identity+memory injection), `a5d247f56` (content-graph tests), `513bfb7d4` (approval event)

**Migrations:** none

**Verification:**
- Type-check: ✅ clean (exit 0)
- Tests: ✅ 8032 pass / 1 pinned known-red pre-existing (`src/land/youtube/__tests__/actions.test.ts:322`) / 34 skipped (+47 new vs baseline 7985)
- Lint: ✅ 11 err / 335 warn == frozen baseline exact
- Build: ✅ exit 0
- Deploy: ✅ SHA 1df573d8 matched on `/api/version` (CF-direct, SKIP_TESTS=1 with WARNING line per deploy-verify rule — known-broken base C1)
- Production smoke: ✅ `/api/health` → 200, `/login` → 200
- Feature smoke: ✅ `/api/creative-identity` unauth → 401, `/api/creative-memory` unauth → 401; webhook probes 503 canary_window = designed version-pinning (known-red, expected)

---

## [2026-08-21] Phase 4 Creative Learning Loop — Test Coverage + Production Bug Fix

**Summary (vi):** Hoàn thành test coverage cho toàn bộ Creative Learning Loop — 5 modules, 70 tests. Phát hiện và sửa lỗi production trong learning-velocity-cron (created_at → recorded_at). Fix schema mismatch giữa shared test shim và production migration 0243.

**Summary (en):** Completed test coverage for the full Creative Learning Loop — 5 modules, 70 tests. Discovered and fixed production bug in learning-velocity-cron (created_at → recorded_at). Fixed schema mismatch between shared test shim and production migration 0243.

**Key Changes:**
- New: `winner-picker.test.ts` — 24 tests (computeCtr, chiSquareTest, evaluateWinner, evaluateBatch)
- New: `performance-aggregation.test.ts` — 8 tests (AGGREGATION_WINDOW_MS, HIGH_CONFIDENCE_THRESHOLD, mergeMetrics)
- New: `experiment-feedback-cron.test.ts` — 5 tests (buildWinnerPayload)
- New: `tracker.test.ts` — 14 tests (recordROI, getWorkspaceROI, getTopROIChannels)
- Updated: `learning-velocity-cron.test.ts` — 19 tests (schema fixed, INSERT columns corrected)
- Fix: `learning-velocity-cron.ts` production bug — two SQL queries used `created_at` but production table uses `recorded_at`
- Exported pure helpers for testability: parseMetrics, avgMetrics, computeVelocityScore, EventRow, mergeMetrics, buildWinnerPayload

**Test Results:** 7112 passed, 34 skipped, 10 todo. Build: 0 errors.

---

## [2026-08-18] Phase 7 Monetization OS — Revenue Attribution, Dynamic Pricing, Affiliate Links, Ad Revenue, ROI Dashboard

**Summary (vi):** Triển khai Monetization OS — tính toán doanh thu theo nội dung (revenue attribution), giá.dynamic theo tier (dynamic pricing), link affiliate theo nội dung, thu nhập quảng cáo BYOK, và dashboard ROI unified. 7 sub-phase, 20+ files, 6940 tests pass.

**Summary (en):** Deployed Monetization OS — per-content revenue attribution, tier-based dynamic pricing, content affiliate links, ad revenue BYOK, and unified ROI dashboard. 7 sub-phases, 20+ files, 6940 tests pass.

**Commits:** `364c6771`, `c7c7388b`, `b821abd9`

**Key Changes:**
- Consolidated duplicate ROI calculators (forest → land re-export barrel)
- Revenue attribution migration + 6-CTE resolver
- Content-to-affiliate link join table + tracking
- Dynamic pricing engine (tier multipliers + volume discounts)
- Ad network BYOK credential storage (Adsense, YouTube Partner, Adsense Legacy)
- `/api/monetization` endpoint with workspace-level authorization
- `/dashboard/monetization` page with summary cards + channel table

**Security Fixes:**
- HIGH-1: IDOR in monetization dashboard — hardcoded `workspaceId=default` replaced with authenticated org lookup via `org_members`
- HIGH-2: `listAdNetworks` returns failure (not empty) on unauthorized access
- HIGH-3: Migration 0248 made idempotent with guard table

**Migrations:** `0246_revenue_attribution.sql`, `0247_content_affiliate_links.sql`, `0248-extend-credential-network-types.sql`

**Verification:**
- Type-check: ✅ clean
- Tests: ✅ 6940/6940 pass
- Build: ✅ exit 0
- Deploy: ✅ SHA b821abd9 matched on `/api/version`
- Production smoke: ✅ `/api/health` → 200, `/vi/login` → 200
- Feature smoke: ✅ `/api/monetization` → 401 (auth required), `/api/roi` → 401 (auth required)

---

## [2026-08-18] i18n Validation Fix — Namespace Hints and Missing Keys

**Summary (vi):** Sửa lỗi i18n validation bằng cách thêm `// i18n-namespace` comment cho các component nhận `t` qua props, và thêm các missing provenance keys.

**Summary (en):** Fixed i18n validation failures by adding `// i18n-namespace` comments for prop-passed `t` components, and adding missing provenance list keys.

**Commit:** `b821abd9`

**Verification:** `i18n:validate` exits 0, all 6940 tests pass.
