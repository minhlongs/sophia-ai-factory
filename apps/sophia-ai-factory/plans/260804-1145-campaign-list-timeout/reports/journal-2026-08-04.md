# Journal — /campaign list Timeout Fix

**Plan:** 260804-1145-campaign-list-timeout
**Date:** 2026-08-04
**Status:** Completed

## What happened
`/campaign list` timed out for users with many campaigns. Root cause: unbounded row fetch (20+) + unbounded MarkdownV2 string construction in `handleCampaignList`.

## What we did
Applied the proven Phase 2 pattern from plan `260804-1102` (status fix):
- Row cap: `.limit(10)`
- Per-title truncation: 80 chars via `truncateMarkdownV2Safely`
- Global message cap: 3800 chars with `(truncated)` footer

## Verification
- Type-check: 0 errors
- Tests: all pass (6694+)
- Code review: approved (3 findings, all resolved)

## Review findings resolved
1. Removed unused `escapeMarkdownV2` import (copy-paste residual)
2. Removed dead `truncate()` helper + constants (never called)
3. Added timeout rationale comment above `MAX_CAMPAIGN_LIST_ROWS`

## Outcome
Telegram `/campaign list` no longer times out. Protected flows unchanged.
