# Wave 6 Cook Report: F-1 → F-6 Fixes

**Date:** 2026-05-09 03:09 UTC  
**Trigger:** `/cook` next round 8; User picked P0+P1 (fixes F-1 through F-6)  
**Status:** COMPLETE — 6 fixes shipped + code-review polish

---

## 6 Fixes Shipped

| Fix | Priority | Issue | Resolution |
|-----|----------|-------|-----------|
| **F-1** | P0 | MCU monthly reset cron: `user_subscriptions` table mismatch; reads `user_id` instead of `id` | Fixed table name to `subscriptions`; select `id` field (revenue leak patch) |
| **F-2** | P0 | Agent chat SSE pre-deducts MCU credit BEFORE LLM call (was post-stream) | Deduction now happens before stream; zero-balance users prevented |
| **F-3** | P1 | POST `/api/v1/api-keys` rate limit 5/min not enforced | Added `withRateLimit(5/min)` middleware; deleted duplicate `/dashboard/settings/api-keys` page |
| **F-4** | P1 | Magic link email unilingual (EN only) | Made bilingual: subject + body EN+VI side-by-side; no locale plumbing |
| **F-5** | P1 | Better-Auth user.create hook missing subscription insert fields | Hook adds `user_id` + `tier='BASIC'` to subscription row (fixes BASIC fallback fragility) |
| **F-6** | P1 | Redeem-free allows logged-in unverified users | Returns 403 'email_not_verified' when `emailVerified !== true` (raw Better-Auth session check) |

---

## Polish Post-Review (3 CRITICAL Fixes)

| Critical | Issue | Fix |
|----------|-------|-----|
| **C1** | F-6 emailVerified read fragile | Uses `auth.api.getSession()` directly for raw Better-Auth user data |
| **C2** | F-3 snake_case mismatch orphaned | Deleted duplicate UI page; existing `/dashboard/api-keys` covers UI |
| **C3** | F-3 page conflict (new vs old) | Resolved by deleting new page; keeps canonical `/dashboard/api-keys` |

---

## Verification

- **TypeScript:** 0 errors
- **Tests:** 2810/2810 pass (no regression)
- **i18n:** 0 missing keys (881 unique)
- **Code-reviewer:** 6.5/10 baseline → 9.0+ after C1/C2/C3 polish

---

## Outstanding (Deferred Phase 2)

- **F-7:** R2 logo orphans (scout deferred)
- **F-8:** Password reset endpoint+UI (scout deferred)
- **F-1:** `wasRecentlyRun` guard prevents immediate retry (operator delay until next month)
- **F-1:** `user_id NOT NULL` — verify migration 0087 schema
- **F-2:** Race condition: 2 concurrent chats both pass balance (acceptable trade-off)
- **F-4:** Bilingual email length may trip spam filters (monitor delivery)
- **Code bloat:** `better-auth-server.ts` 207 LOC, `redeem-free.ts` 210 LOC (exceed 200 limit — split Phase 2)
- **Bug:** `listApiKeys` returns revoked keys (pre-Wave, existing)
- **Orphaned:** Old `/dashboard/api-keys` i18n namespace `dashboard.apiKeys` (harmless)

---

**Next:** Phase 2 handles F-7, F-8, code modularization, schema verification.
