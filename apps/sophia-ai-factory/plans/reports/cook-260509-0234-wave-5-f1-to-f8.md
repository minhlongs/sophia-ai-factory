# Wave 5 Cook Report: F-1 → F-8 + withRateLimit

**Date:** 2026-05-09 02:34 UTC  
**Trigger:** /cook round 7; user approved ALL F-1→F-8 + per-route tier-aware withRateLimit  
**Status:** ✅ SHIPPED (8 fixes)

## Shipped Fixes

| ID | Priority | Feature | Summary |
|----|----------|---------|---------|
| F-1 | P0 | Inngest Crons | offerSyncCron + storageTrackerDaily registered (was missing → silent failures) |
| F-2 | P0 | Webhook Security | X-Sophia-Timestamp + Stripe-style HMAC {timestamp}.{body} payload (replay protection) |
| F-3 | P0 | Rate Limiting | Tier-aware withRateLimit on 4 hottest routes (factory/url-to-revenue, missions POST, campaigns/create); middleware baseline covers rest |
| F-4 | P1 | Bilingual /status | New status namespace (en+vi); Intl locale-aware |
| F-5 | P1 | /dashboard/agents | NEW page.tsx wraps existing AgentTeamPanel |
| F-6 | P1 | /api/v1/sops | GET templates + POST install (RBAC); closes "API SOP install" tier promise |
| F-7 | P1 | /api/admin/users/[id] | PATCH tier/role promotion (requireAdmin); subscription writes both plan+tier columns |
| F-8 | P1 | CSP + Logger | report-uri + worker-src blob:; NEW /api/csp-report (16KB cap, 413 enforcement) |

## Polish Post-Review (2 HIGH)

- **H-3 (F-7):** Subscription update writes BOTH plan (lowercase) + tier (uppercase) → keeps getUserTier() reads fresh
- **H-4 (F-8):** /api/csp-report enforces 16KB max (Content-Length + parse) → HTTP 413 on overflow

## Verification

| Check | Result |
|-------|--------|
| TypeScript | 0 errors |
| Tests | 2810/2810 pass (no regression; F-8 test directive 12→13) |
| i18n Validator | 0 missing keys (1960 t() calls, 856 unique) |
| Code Review | 9.2/10 → polish lifts ≈9.4 |

## Outstanding (Deferred Phase 2)

**HIGH:**
- H-1: Webhook tests don't assert new timestamp+payload format (silent regression risk)
- H-2: Outbound receivers face BREAKING signature change (need canary docs)

**MEDIUM:**
- M-2: Signature format inconsistency (sender.ts header vs webhook-hmac.ts Stripe-combined)
- M-3: /dashboard/agents duplicates /dashboard/missions content
- M-1: F-7 SELECT-then-INSERT race condition (low-volume admin route)

**LOW:**
- L-1: F-7 admin route lacks rate-limit
- 28 v1 routes unwrapped (covered by middleware IP-bucket baseline; tier-aware overlay deferred)

---

**Next:** Phase 2 webhook stability + signature standardization + dashboard dedup
