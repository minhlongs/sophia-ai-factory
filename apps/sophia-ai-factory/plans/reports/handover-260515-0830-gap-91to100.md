# Handover v2 — Sophia AI Factory: 91 → 100/100 + Divergence Reconciliation

**Date:** 2026-05-15 08:30 PT
**Last updated:** 2026-05-15 08:40 PT (sync status patch)
**Supersedes:** `handover-260513-0549-gap-90to100.md`
**Verified score:** **91/100** (post code-grooming sprint v1.26.9)
**Critical state issue:** Production SHA ≠ git origin SHA — see §0.

## Status Update (2026-05-15 08:40 PT)

- Handover v2 itself shipped as commit `7ca17e83` and pushed to both mirrors.
- Parallel session committed `5cd07d70` (feat: ShareASale + Awin + Rakuten scout clients — Phase 02) on local HEAD mid-push.
- Both mirrors synced: `origin/main = gitlab/main = HEAD = 5cd07d70`.
- **Divergence still applies:** production stays at `9da5a1b7`. The parallel session's compliance + scoring commits (`212b6960`, `9da5a1b7`) remain reflog-only. The new scout-clients commit (`5cd07d70`) is on git but NOT yet deployed to prod.

So now there are **two** prod/git deltas:
1. **Prod-only** (in worker but not git): scoring + compliance Phase 08 (the original §0 issue).
2. **Git-only** (in git but not prod): handover docs (harmless) + my grooming `f3775a8e` (harmless code/migration; migration ran on prod DB directly) + scout-clients `5cd07d70` (NEW — must deploy via `npm run deploy:full` to take effect).

Next deploy is risky — it will UPDATE production to `5cd07d70` which includes my grooming + scout clients but OVERWRITE the live scoring + compliance from `9da5a1b7`. **DV-1 reconciliation MUST happen before next `npm run deploy:full`.**

---

## 0. CRITICAL: Production / Git Divergence (READ BEFORE DOING ANYTHING)

Running `curl https://sophia.agencyos.network/api/version` returns:
```
shortSha: 9da5a1b7
```

But `git rev-parse origin/main | cut -c1-8` returns:
```
f3775a8e
```

**These do NOT match.** Production runs commits that are NOT on origin/main.

### Reconstruction of how we got here

1. Phase 5.1 (`8d525481`) deployed + pushed to origin 2026-05-13.
2. Parallel CC CLI session worked on different features 2026-05-14:
   - `90ed3735` feat(scoring): anti-scam gate + EPC + crypto volume factors
   - `9da5a1b7` feat(compliance): Phase 08 — per-jurisdiction crypto disclaimer + KYC notice
3. Parallel session ran `npm run deploy:full` → **CF-direct doctrine pushed `9da5a1b7` to production**. They did NOT `git push` to origin/gitlab.
4. Another parallel commit `b0b34ffd` chore(sophia): align support/billing/admin DID get pushed to origin (someone else's session).
5. My session today (2026-05-15) did code grooming → commit `1db2b8b2` locally.
6. Tried to push → conflict because origin had `b0b34ffd` I didn't have.
7. Rebased my commit onto `b0b34ffd` — chain became `b0b34ffd → 212b6960 → 9da5a1b7 → 11ac6e6d`.
8. Pre-push hook flaked on `writer.test.ts` (test pollution in scout/__tests__/).
9. Per user direction: reset to `origin/main` + cherry-pick only my commit → `f3775a8e`.
10. **Dropped 212b6960 + 9da5a1b7 from git origin** (they remain in reflog only).
11. Pushed `f3775a8e` to origin + gitlab.

### Result — DELTA between prod and git

| State | Has | Missing |
|-------|-----|---------|
| **Production worker** | scoring upgrades, Phase 08 compliance | my grooming (CA-1..5) — except CA-1 DB drop which I executed via wrangler d1 directly |
| **git origin/main** | my grooming (CA-1..5) | scoring upgrades, Phase 08 compliance — only in reflog |
| **Local working tree** | nothing extra (all stashed) | nothing checked out |

### Recovery options (DO NOT CHOOSE UNILATERALLY — needs user/owner input)

**Path A: Recover parallel work from reflog, push to origin**
```bash
# Get reflog hashes (these MAY have aged out — verify within 90 days)
git reflog | grep -E "212b6960|9da5a1b7"

# Cherry-pick onto current HEAD
git cherry-pick 212b6960 9da5a1b7

# Push (will need to fix writer.test.ts flake first OR --no-verify)
git push origin main
```
Risk: writer.test.ts test pollution returns; need to debug or bypass.

**Path B: Owner of parallel session deploys + re-creates commits**
Wait for the parallel session owner to come back, run `git reflog` themselves, decide what to push.
Risk: parallel work fades from reflog after 90d; may need re-implementation.

**Path C: Treat production as truth, re-extract**
Inspect deployed worker code from `.open-next/` (last build), reverse-engineer commits, re-stage on top of `f3775a8e`.
Risk: high. Build artifacts != source of truth.

**My recommendation:** Path A by parallel session owner. They wrote the code, they know the intent, they should resolve the flake before pushing.

---

## 1. State Snapshot

| Item | Value |
|------|-------|
| Production SHA | `9da5a1b7` (parallel session, NOT in git — reflog-only) |
| Git origin/main | `5cd07d70` (handover v2 + grooming + scout-clients Phase 02) |
| Git gitlab/main | `5cd07d70` (synced) |
| HEAD local | `5cd07d70` |
| Verified fullstack score | **91/100** |
| Test baseline | 4169 vitest pass standalone (full suite) |
| Pre-push test flake | `src/lib/affiliates/scout/__tests__/writer.test.ts` (pollution from concurrent suites) |
| ESLint baseline | 423 warnings (full-project, post-Phase-4/5.1 net +2) |
| HIGH npm vulns | 0 |
| Stashes preserved | 23 entries (mine: stash@{0,1,2} from this session) |
| Parallel session activity | Active — committed `5cd07d70` mid-push 08:35 PT |

**Layer scoreboard (post-grooming v1.26.9):**
| L1 DB | L2 Srv | L3 Net | L4 Cloud | L5 CI | L6 Sec | L7 Mon | L8 Cnt | L9 CDN | L10 Bak |
|-------|--------|--------|----------|-------|--------|--------|--------|--------|---------|
| 7 | 9 | 9 | **9.5** | **9.5** | 9 | 8 | 10 | 9 | 7 |

Bolded = improved vs prior handover:
- L4 +0.5 — cross-layer exemption now documented inline (CA-2)
- L5 +0.5 — ESLint baseline accurate to reality (CA-3)

---

## 2. Path 91 → 100/100 (Still +9 max, realistic ~+8 to 99)

### 2.1 Operator-Blocked (UNCHANGED from v1)

| ID | Action | Layer | Pts | Status |
|----|--------|-------|----:|--------|
| **OP-1** | Register Upstash QStash cron (provide QSTASH_TOKEN, run `scripts/dr/configure-upstash-qstash.sh`) | L1+L10 | +1 | Waiting on user creds |
| **OP-2** | Export SENTRY_AUTH_TOKEN in deploy shell | L7 | +1 | Waiting on user creds |
| **OP-3** | Verify BETTER_STACK_HEARTBEAT_URL secret set | L7 | +0 | 30s wrangler secret list check |

### 2.2 Time-Gated (UNCHANGED)

| ID | Action | Layer | Pts | Earliest |
|----|--------|-------|----:|----------|
| TG-1 | DMARC p=none → p=quarantine | L3 | +0.5 | 2026-06-12 |
| TG-2 | DKIM verify post first email send | L3 | +0.5 | After first prod email |

### 2.3 **NEW: Divergence Reconciliation (PRIORITY)**

| ID | Action | Layer | Pts | Blocker |
|----|--------|-------|----:|---------|
| **DV-1** | Resolve git/prod SHA mismatch — Path A/B/C per §0 | All | 0 (but unblocks future work) | Needs parallel session owner |
| **DV-2** | Fix `writer.test.ts` test pollution so pre-push hook stops flaking — investigate ordering, shared state in `scout/__tests__/` | L5 | +0.5 | None — can do today |

### 2.4 Remaining Code-Actionable (post grooming)

Already done: CA-1, CA-2, CA-3, CA-4, CA-5. Net +1 pt achieved.

New items surfaced this session:
| ID | Action | Layer | Pts | Effort |
|----|--------|-------|----:|--------|
| **CN-1** | Audit + fix root cause of pre-push test flakiness in writer.test.ts (DV-2 above) | L5 | +0.5 | 1h |
| **CN-2** | Apply migration `0109-drop-orphan-revalidations-table.sql` to staging/preview D1 if those exist (prod was done directly via wrangler) | L1 | 0 | 5min |

### 2.5 Future Work (UNCHANGED — see v1 §2.4)

DR drill, monthly restore, CF billing alert, PostCSS monitor, react-compiler revisit, G17/G18/G20 low-sev.

**Realistic ceiling: 99/100** (sustained ops + time-gated wait).

---

## 3. Resume Protocol — First 5 Minutes

```bash
# 1. Verify divergence still exists (or someone fixed it)
LOCAL_SHA=$(git rev-parse origin/main | cut -c1-8)
PROD_SHA=$(curl -s https://sophia.agencyos.network/api/version | grep -o '"shortSha":"[^"]*"' | cut -d'"' -f4)
echo "Local: $LOCAL_SHA  Prod: $PROD_SHA"
# If $LOCAL_SHA != $PROD_SHA → §0 still applies, read it first

# 2. Check unpushed reflog commits still recoverable
git reflog | grep -E "212b6960|9da5a1b7" | head -5
# If empty → commits aged out; owner must re-implement

# 3. Check stashes from this session
git stash list | head -5
# stash@{0..2} are mine, others may be from prior sessions

# 4. Inspect handover (this doc)
cat plans/reports/handover-260515-0830-gap-91to100.md
```

---

## 4. Suggested Next-Session Triage (priority order)

1. **DV-1 (divergence resolution)** — coordinate with parallel session owner. Until resolved, any redeploy will OVERWRITE production with whichever commits are on origin. **Risk: if I redeploy from `f3775a8e` right now, prod LOSES scoring + compliance code.** This is a P0 latent landmine.
2. **DV-2 (writer.test.ts flake)** — even after divergence resolved, future pushes will keep failing intermittently. Fix the test pollution.
3. **OP-1 + OP-2** — operator-blocked items, +2 pts. Need user creds.
4. **TG-1 + TG-2** — time-gated, no action needed now.
5. **Future work** — DR drill, monitoring polish, etc.

---

## 5. Things That Will Trip Up Next Session

(All items from v1 §5 still apply. Adding:)

8. **PROD ≠ GIT** as of this handover. Any `npm run deploy:full` from origin/main HEAD will overwrite production with `f3775a8e`, LOSING the scoring + compliance Phase 08 work currently live. **Verify §0 status before any deploy.**
9. **Pre-push hook is flaky** due to test pollution in `scout/__tests__/`. Standalone test passes; full-suite via hook fails ~50% of the time. Until DV-2 fixes root cause, expect to retry pushes or use `--no-verify` on mirror-only operations (with user approval).
10. **Stash discipline** — I left 3 stashes (`stash@{0,1,2}`) preserving parallel work. Do NOT `git stash clear` blindly. Parallel session owner needs to inspect + pop selectively.
11. **Migration 0109 is in git but applied to prod via wrangler d1 execute directly** — schema state is consistent, but if a fresh D1 clone is ever made for staging/preview, run the migration there too (CN-2).

---

## 6. Reference Files (UPDATED)

| Topic | File |
|-------|------|
| **This handover (v2)** | `plans/reports/handover-260515-0830-gap-91to100.md` |
| Prior handover (v1) | `plans/reports/handover-260513-0549-gap-90to100.md` |
| Code grooming changelog | `docs/project-changelog.md` v1.26.9 |
| Migration 0109 | `migrations/0109-drop-orphan-revalidations-table.sql` |
| Pre-push hook | `.husky/pre-push` |
| Flaky test | `src/lib/affiliates/scout/__tests__/writer.test.ts` |
| Deploy procedure | `scripts/deploy-with-sha.sh` |
| Cross-layer rule | `.claude/rules/cross-layer-orchestration.md` |

---

## 7. Unresolved Questions for Operator (UPDATED)

1. **DV-1 reconciliation strategy?** — Path A (cherry-pick from reflog) is safe but needs the test flake fixed. Path B (wait for owner) is safer but slower. Path C (reverse-engineer from build) is last resort.
2. **OP-1/OP-2 creds?** — Will user provide QSTASH_TOKEN + SENTRY_AUTH_TOKEN this session or defer?
3. **DV-2 timing?** — Fix writer.test.ts pollution before next push attempt (1h), or accept --no-verify as standard workaround?
4. **Should `9da5a1b7` features be redeployed?** — They're live on production but lost from git. If we cherry-pick + push, we need to re-deploy to confirm prod == git. Risk: brief window of churn.
5. **Many old stashes (23 entries) in `git stash list`** — clean up old ones (older than 30d)? Some are from `feat/phase-10-publisher` branch, may be obsolete.

---

**End of v2 handover.**
- Original sign-off: git `f3775a8e` / prod `9da5a1b7` (DIVERGENT — see §0).
- Post-sync patch (08:40 PT): git `5cd07d70` (origin == gitlab) / prod `9da5a1b7` (STILL DIVERGENT — DV-1 unresolved).
