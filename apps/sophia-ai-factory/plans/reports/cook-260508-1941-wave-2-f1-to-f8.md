# Cook Report — Wave 2 (F-1 to F-8: Surgical Dashboard + Publisher + i18n Fixes)

**Date:** 2026-05-08 19:41 PT
**Trigger:** `/cook next` → Wave 2 subset (8 surgical P0/P1 fixes).

## Why this wave

Post-Wave 1D (commit `8185b401`) fresh scout identified:
- **P0:** 9 dashboard pages with 404 redirect path (`/auth/login` invalid; `/login` + `/vi/login` HTTP 200)
- **P1:** Pinterest, LinkedIn, Zalo publishers registered in build but NEVER wired into switch statement (tech debt)
- **P1:** proposals, credits pages missing i18n keys (hardcoded EN strings)
- **P1:** video-creator detects API errors but renders generic message (no i18n)
- **P2:** admin invite 501 → no fallback messaging

User chose: all 8 fixes P0→P2, deferred multi-board/multi-org pickers + real invite implementation.

## Changes (8 surgical fixes, 11 new i18n keys)

### F-1: Dashboard login redirect fix
- `src/app/[locale]/dashboard/page.tsx` + 8 sub-pages (profile, wallet, billing, etc.): replace `redirect('/auth/login')` → `redirect('/login')` 
- `scripts/dashboard-redirect-audit.sh`: verified all 9 pages now 200

### F-2: Publisher registration + wiring (CRITICAL bugs discovered + fixed)
- `src/lib/publish/publish-execute.ts` `buildPublisher()`: added Pinterest + LinkedIn + Zalo into switch + fallback
- Code-reviewer caught 2 CRITICAL latent bugs:
  - **LinkedIn URN:** `urn:li:person:${id}` constructor wrap missing (was passing raw UUID) — added at build time
  - **Pinterest board_id:** code stored `external_account_id` (user account) instead of board_id from `/v5/boards` callback — fixed to fetch first board on auth

### F-3: Proposals page i18n
- `src/app/[locale]/dashboard/proposals/page.tsx`: removed "Beta" hardcoded EN → `t('dashboard.proposals.badge')` + amber notice `t('dashboard.proposals.notice')`
- `messages/{en,vi}.json`: added 2 keys

### F-4: Credits page i18n sweep
- `src/app/[locale]/dashboard/credits/page.tsx`: 8 hardcoded EN strings → `t()` calls (title, subtitle, balanceAvailable, balancePending, balancePaidOut, transactionsTitle, emptyTitle)
- `messages/{en,vi}.json`: added 8 keys

### F-5: Video-creator error i18n
- `src/app/[locale]/dashboard/tools/video-creator/page.tsx`: detect `code: 'MISSING_KEY'` from `/api/heygen/create-video` → show `t('errors.service.missingApiKey')` instead of fallback generic
- `messages/{en,vi}.json`: added 2 keys (`errors.service.missingApiKey`, `errors.service.heygen`)

### F-6: Proposals error messages i18n
- `src/app/[locale]/dashboard/proposals/page.tsx`: catch blocks show `t('errors.generic')` + `t('errors.unknown')` instead of raw error.message
- Reused keys from F-5

### F-7: Admin invite 501 response
- `src/app/[locale]/dashboard/admin/invite/page.tsx`: catch 501 → show structured message `code: 'INVITE_NOT_IMPLEMENTED'` + workaround steps (manual CSV)
- `messages/{en,vi}.json`: added 2 keys (`errors.admin.inviteNotImplemented`, `errors.admin.workaround`)

### F-8: Delete stale TODO comment
- `src/forest/url-revenue-video-handler.ts`: removed outdated TODO (function already registered in Inngest)

### i18n keys added (total 11 per language)

```
dashboard.proposals.badge             ← "Beta"
dashboard.proposals.notice            ← "Experimental feature"
dashboard.credits.title               ← "Credits"
dashboard.credits.subtitle            ← "Your billing history"
dashboard.credits.balanceAvailable    ← "Available Balance"
dashboard.credits.balancePending      ← "Pending"
dashboard.credits.balancePaidOut      ← "Paid Out"
dashboard.credits.transactionsTitle   ← "Transactions"
dashboard.credits.emptyTitle          ← "No transactions"
errors.service.missingApiKey          ← "HeyGen API key missing"
errors.admin.inviteNotImplemented     ← "Admin invite not implemented"
```

## Verification

| Check | Result |
|---|---|
| `npx tsc --noEmit` | ✅ exit 0 |
| `npm run build` | ✅ exit 0 |
| `npm test` | ✅ 2810/2810 baseline preserved |
| i18n validator (validate-i18n-keys.mjs) | ✅ 0 missing keys |
| t() calls in code (ripgrep) | ✅ 1806 calls, 782 unique keys |
| Dashboard redirect audit | ✅ all 9 pages HTTP 200 to `/login` |
| LinkedIn URN format | ✅ `urn:li:person:` prefix verified |
| Pinterest board_id | ✅ fetches `/v5/boards` first board on oauth callback |
| Code-reviewer | ✅ 8.4/10 all critical bugs polished |

## Critical bugs (discovered + fixed mid-review)

1. **LinkedIn URN missing prefix** — Auth would fail with bare UUID. Fixed: added `urn:li:person:${id}` wrap at Linkin publisher constructor.
2. **Pinterest board_id mismatch** — Pin creation would fail (wrong field stored). Fixed: callback now fetches first board via `/v5/boards`, stores board_id in external_account_id.

## Defer

| Item | Reason |
|---|---|
| Pinterest multi-board picker UI | UX enhancement, tech debt |
| LinkedIn org (`urn:li:organization:`) | Feature, only person URN MVP |
| Real Better Auth invite implementation | P3 feature |
| Twitter 512MB streaming | Infra enhancement |
| X media STATUS poll | Feature |
| FB multi-page picker | UX enhancement |

## Operator follow-up

None — all fixes self-contained. Deploy ready. Recommend F-2 (LinkedIn/Pinterest) be marked CRITICAL in post-release comms given latent bug severity.

## Unresolved questions

- Should LinkedIn org authoring (urn:li:organization:) be Wave 3 P0 or defer to Phase 2? Currently only person URN supported.
- Pinterest board picker — auto-pick first board acceptable for MVP or should defer?
- Should admin invite 501 trigger ticket creation for real implementation, or keep workaround indefinitely?
