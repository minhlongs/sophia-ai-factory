# F01 Remediation Report — 260518-0641

## Files Changed/Created

- MODIFIED: `src/seed/security/sql-rate-limiter.ts` — added `checkAccountLock`, `incrementFailedLogin`, `resetFailedLogin`, `checkAccountLockByUserId`, `D1Binding` (exported), constants `ACCOUNT_LOCK_THRESHOLD=5`, `ACCOUNT_LOCK_DURATION_MS=24h`
- CREATED: `src/seed/auth/account-lockout-hook.ts` — `verifyWithLockout()` wrapper; re-exports 3 core functions + TODO wiring note
- CREATED: `migrations/0114-user-failed-logins.sql` — migration file
- CREATED: `src/security-tests/f01-per-account-rate-limit.test.ts` — 14 tests

## Migration Filename

`migrations/0114-user-failed-logins.sql` (next after 0113-video-jobs-completed-at.sql)

Adds to `"user"` table:
- `failed_login_attempts INTEGER NOT NULL DEFAULT 0`
- `locked_until INTEGER` (unix ms, NULL = unlocked)
- Index on `locked_until WHERE locked_until IS NOT NULL`

## Test Results

14/14 passed. Duration ~530ms.

## Wiring Approach

**Wrapper-with-TODO approach.** Better Auth's `emailAndPassword.password.verify` callback does NOT inject `userId` — it only receives `{ hash, password }`. Clean hook wiring is therefore not possible without Better Auth upstream support.

`account-lockout-hook.ts` exports `verifyWithLockout(db, userId, verifyFn)` with inline TODO pointing to where wiring should occur when Better Auth adds `beforeSignIn` / `afterSignInFailed` callbacks.

The three core functions (`checkAccountLock`, `incrementFailedLogin`, `resetFailedLogin`) are production-ready and tested.

## Lint/Typecheck

- TypeScript (F01 files): 0 errors
- Lint (F01 files): 0 warnings
- Pre-existing unrelated error: `src/seed/auth/require-admin.ts` (F02 agent's file, Uint8Array/ArrayBuffer TS lib mismatch — not introduced by F01)
- Total lint baseline: 342 warnings (pre-existing; 0 from F01 files)

## Unresolved Questions

1. When Better Auth exposes login result callbacks (e.g., `afterSignInFailed`), wire `incrementFailedLogin` there and `checkAccountLock` before password check in `better-auth-server.ts`. Tracked via TODO in `account-lockout-hook.ts`.
2. Migration `0114` must be applied manually: `npx wrangler d1 execute sophia-raas-db --file=migrations/0114-user-failed-logins.sql --remote`
