# F03 Remediation Report — IDOR /api/admin/promo-codes/[id] (ASVS V4.3.1)

**Date:** 2026-05-18
**Agent:** Phase 06 F03 executor

## Reconnaissance

| Check | Result |
|---|---|
| `[id]/route.ts` exists? | NO — only sub-routes exist: `[id]/status/route.ts` (PATCH) + `[id]/redemptions/route.ts` (GET) |
| `promo_codes.agency_id` column? | NO — table is global/single-tenant (migration 0066). Schema has `created_by_admin_id` only. |
| Tenant-isolation middleware covers `promo-codes/[id]`? | NO — covers `/api/admin/licenses/[id]` and usage routes only. |

## Finding Status: **N-A**

`promo_codes` is a single-tenant platform-wide table. No multi-tenant boundary exists to cross. IDOR between agencies is architecturally impossible — the ASVS desk-review was a false alarm.

## Files Changed

| File | Action |
|---|---|
| `src/app/api/admin/promo-codes/[id]/route.ts` | Not modified (does not exist — N-A) |
| `src/security-tests/f03-promo-idor.test.ts` | CREATED — 2 skipped (N-A finding documented) + 6 active admin-gate regression tests |

## Test Results

```
Test Files  1 passed (1)
     Tests  6 passed | 2 skipped (8)
   Duration  454ms
```

## Typecheck / Lint

- `pnpm tsc --noEmit`: 0 errors in f03 file (pre-existing errors in `require-admin.ts` unrelated)
- `pnpm lint --max-warnings 342`: passes (342 = baseline, 0 new warnings from f03 file)

## Unresolved

- If multi-tenancy is added to `promo_codes` in future, a migration adding `agency_id` column + scoping of `updateCodeStatus`/`listRedemptionsByCode` + sub-route handlers will be required. TODOs documented in the test file.
