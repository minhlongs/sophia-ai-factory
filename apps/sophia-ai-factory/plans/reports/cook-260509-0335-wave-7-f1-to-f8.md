# Wave 7 Cook Report: F-1→F-8 + AES-GCM TOTP Encryption
**Date:** 2026-05-09 03:35  
**Trigger:** /cook next round 9; user approved F-1→F-8 + AES-256-GCM TOTP encryption

---

## 8 Fixes Shipped

| ID | Priority | Change |
|----|----------|--------|
| **F-1** | P0 | TOTP secrets encrypted AES-256-GCM at-rest. Setup writes encrypted; verify/disable/challenge decrypt pre-verifyTotp. Legacy plaintext rows fallback (auto-migrate on re-enroll). |
| **F-2** | P0 | SOP detail page: `actionFn.bind(null, id)` server-action binding replaces inline arrow funcs (RSC→Client boundary fix). |
| **F-3** | P1 | api-keys [id]/rotate POST + [id] DELETE inline rate-limit (5/min rotate, 10/min revoke). |
| **F-4** | P1 | usage/batch POST wrapped withRateLimit 10/min. |
| **F-5** | P1 | Drop image/svg+xml from branding upload allowlist (XSS prevention). |
| **F-6** | P1 | NEW POST /api/user/byok/test — pings provider endpoints (/models or /user). Supports OpenRouter, Anthropic, ElevenLabs, D-ID, MuAPI; skips heygen (server-only). |
| **F-7** | P1 | Mission SSE: heartbeat ping 15s + safeParseResult guard + surface db_transient errors instead of silent swallow. |
| **F-8** | P1 | Webhook test inline rate-limit 5/min + SSRF gate comment (sender.ts owns blocklist). |

---

## Polish Post-Implementation (Test Compatibility)

- `rate-limit-wrapper.ts`: URL parse resilience (try/catch fallback to '/')
- `rate-limit-wrapper.ts`: Duck-type instanceof guard (test mocks shim NextResponse as plain object)

---

## Verification

- **tsc:** 0 errors
- **Tests:** 2810/2810 (no regression after wrapper polish)
- **Code-reviewer:** PENDING

---

## Deferred (Phase 2)

- F-1 backfill: plaintext rows auto-migrate on re-enroll; no DB migration
- F-7 Last-Event-ID reconnect support
- BYOK test endpoint UI wiring (SettingsApiKeys page)
- listApiKeys revoked filter (pre-existing gap)

---

**Status:** Ready for code review. Wave 8 queued.
