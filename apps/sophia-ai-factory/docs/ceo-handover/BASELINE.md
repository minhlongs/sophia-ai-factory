# SOPHIA CEO HANDOVER — PROTECTED BASELINE

**Audit:** FOUNDER → CEO TRANSITION
**Date:** 2026-09-02
**Baseline SHA:** `5dd1f071da4f78280d8c69d2ce6bdbcec84bf1af` (short: `5dd1f071`)

---

## Repository State

| Field | Value |
|---|---|
| Current branch | `main` |
| HEAD | `5dd1f071d` |
| Working tree | CLEAN (no uncommitted changes) |
| Diff vs baseline | EMPTY (no drift) |
| Commit message | `fix(build): resolve 73 TS errors in cron route tests and alert types` |

## Deployment Status

| Check | Result |
|---|---|
| Deploy method | CF-direct (`npm run deploy:full`) |
| Local SHA | `5dd1f071` |
| Live SHA (`/api/version`) | `5dd1f071` |
| SHA match | ✅ PASS |
| Production URL | `https://sophia.agencyos.network` |
| `/api/health` | ✅ 200 |
| `/login` | ✅ 307 → 200 (locale-prefix redirect — expected) |
| `/vi/login` | ✅ 200 (verified in prior deploy) |

## Known Expected Redirects

- `GET /` → `307` redirect to `/{locale}/...` (locale-prefix routing, `next-intl`, `localePrefix: 'always'`, default `vi`). **Not a defect.** Per `sophia-deploy-verify.md` override: verify `/api/health` and `/login` instead of root.
- `GET /login` → `307` → `200` after follow.

## Test Baseline

| Metric | Value |
|---|---|
| Test files | 849 passed / 1 skipped (850) |
| Tests passed | 8,757 |
| Tests skipped | 34 |
| Tests todo | 10 |
| Failures | 0 |

## Build Status

| Check | Result |
|---|---|
| `npm run build` | ✅ exit 0, 0 TypeScript errors |
| `npm run lint` | pending audit |
| `:any` types | 0 (per repo standard) |
| `eslint-disable` additions | 0 (suppression freeze respected) |

## Protected Flows (DO NOT BREAK)

1. **Setup Wizard** — BYOK API key onboarding (OpenRouter, ElevenLabs, D-ID, HeyGen)
2. **Telegram Bot** — `@Sophia_Bbot` commands (`/campaign`, `/status`, `/results`)
3. **Payment Flow** — NOWPayments IPN webhook → tier activation (PayOS backup; Polar.sh and PayPal BANNED)

## Production Dependencies (to be audited in Phase 2)

- Cloudflare Workers (OpenNext build) — `.open-next/worker.js`
- Cloudflare D1 — `sophia-raas-db` (binding `DB`)
- Cloudflare R2 — `sophia-ai-factory-opennext-cache` (binding `NEXT_INC_CACHE_R2_BUCKET`)
- Inngest — long-running workflows (video generation, multi-step processes)
- Sentry SDK — error capture (source map upload opt-in, `SENTRY_AUTH_TOKEN` optional)
- AI providers (BYOK): OpenRouter, Anthropic, OpenAI, ElevenLabs, D-ID, HeyGen, Replicate, fal.ai
- Payment: NOWPayments (primary), PayOS (Vietnam domestic backup)
- Telegram Bot API
- next-intl (i18n, locale segment `[locale]`, default `vi`)

## Doctrine Constraints (active)

- **CF-direct:** GitHub Actions disabled by design; deploy via local `wrangler CLI`
- **No-tech / BYOK:** Customers self-input all third-party credentials; operator manages platform only
- **4-layer architecture:** `seed` → `tree` → `forest` → `land` (import direction enforced)
- **Banned imports:** `@/lib/auth`, `@/lib/subscription`, `@/lib/unified-tier-config`, `@/lib/tier-gate`
- **Tier enum:** `BASIC | PREMIUM | ENTERPRIUM | MASTER` (uppercase only)
- **Bilingual:** Vietnamese + English required for all customer-facing content

## Audit Scope

This baseline is the protected reference. No production behavior may change without:
1. A verified operational risk, AND
2. Minimal, reversible change, AND
3. Existing tests, AND
4. Protected flows unchanged (or documented deviation)

---

*Generated by CEO HANDOVER AUDIT, Phase 0.*