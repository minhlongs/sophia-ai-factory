/**
 * /ref — Affiliate referral lookup routes.
 * Minimal stub layout — content lives in the page components.
 */

import React from 'react';

export default function RefLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
