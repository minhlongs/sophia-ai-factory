/**
 * /creator — Root layout for creator dashboard routes.
 * Minimal stub — the real layout shell lives in the page component.
 */

import React from 'react';

export default function CreatorLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
