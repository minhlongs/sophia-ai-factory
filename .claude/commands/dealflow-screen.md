---
description: "Screen deal against investment thesis — five-factor quick eval. 1 command, ~5 min."
argument-hint: [deal-id or --all-new]
allowed-tools: Bash
---

# /dealflow:screen — Deal Screening

## Engine command

```bash
mekong dealflow screen $ARGUMENTS
```

## Fallback

If engine not ready, run `mekong --help` to check installation, then retry.

## Goal context

<goal>$ARGUMENTS</goal>
