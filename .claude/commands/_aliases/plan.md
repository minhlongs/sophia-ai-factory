---
description: ⚡⚡⚡ Redirect to /core/plan (backward-compat alias for moved plan.md)
argument-hint: [task]
---
> **Alias:** This command has moved to `.claude/commands/core/plan/index.md`. The invocation `/plan` continues to work via this redirect.
> New canonical path: `/core/plan:index` or simply `/plan` (this alias).
