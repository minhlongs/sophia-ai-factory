---
description: ⚡🤖 AUTO Bootstrap AGI RaaS — parallel + zero-prompt — CTO injects this
argument-hint: [optional-service-description]
---

**Ultrathink parallel AUTO mode** — zero human input required.

<requirements>$ARGUMENTS</requirements>

**IMPORTANT:** This is AUTO mode. DO NOT ask questions. DO NOT wait for input. Decide and execute.
**IMPORTANT:** Activate needed skills. YAGNI, KISS, DRY. Tiếng Việt output.
**IMPORTANT:** All subagents use default model from settings.json. Do NOT use `/model` command.

## AUTO Protocol

1. **NEVER** ask "Bạn muốn làm gì tiếp theo?" — just do the next logical thing
2. **NEVER** present options — pick the best one and execute
3. **NEVER** wait for confirmation — commit and continue
4. **IF** tests pass → commit → next phase
5. **IF** tests fail → debug → fix → retry (max 3) → skip if still failing
6. **IF** no $ARGUMENTS → scan codebase → find highest-impact task → execute

## Workflow (Fully Autonomous)

### 1. Context Loading (5s max)
- Read `./CLAUDE.md` for project rules
- Read active plans in `plans/` or project-specific `*/plans/`
- Identify: what's DONE, what's PENDING, what's BROKEN

### 2. Auto-Select Task
If $ARGUMENTS provided → use that as task description.
If empty → scan for:
  1. Failing tests → fix first
  2. Build errors → fix second  
  3. Lint warnings → fix third
  4. Pending plan phases → implement next
  5. Tech debt → address
  6. New feature from backlog → implement

### 3. Parallel Research (max 2 agents, 30s budget)
- Only if task requires new knowledge
- Skip if task is fix/refactor/test

### 4. Parallel Implementation
- Launch multiple `fullstack-developer` agents SIMULTANEOUSLY
- Each agent has exclusive file ownership
- Run `npx tsc --noEmit` after each phase
- Auto-commit with conventional commit message after each phase passes

### 5. Auto-Verify
```bash
# Build
npm run build 2>&1 | tail -5

# Tests  
npm test 2>&1 | tail -10

# Lint
npm run lint 2>&1 | tail -5
```

### 6. Auto-Commit (if all green)
```bash
git add -A
git commit -m "feat: <auto-generated-message>"
```

### 7. Report (compact, no questions)
- Files changed
- Tests: PASS/FAIL count
- Score before → after
- Next auto-task (don't ask, just state what's next)
