# Tier-1 Backend/CI/DevOps Fixes — Report
Date: 2026-04-28 | Audit ref: audit-260428-0253-go-live-100

## Files Changed

| File | Change |
|------|--------|
| `.github/workflows/test.yml` | T1: SHA-pin checkout+setup-node; remove continue-on-error from Lint+Test |
| `.github/workflows/d1-backup.yml` | T2: SHA-pin actions; add R2 off-site upload step; export DATE to env |
| `apps/sophia-ai-factory/next.config.ts` | T3: Add `/_next/static/:path*` + `/static/:path*` immutable cache headers |
| `apps/sophia-ai-factory/src/lib/security/content-security-policy-configuration.ts` | T4: Remove `https://*.supabase.co` from connect-src; add TODO nonce migration |
| `apps/sophia-ai-factory/src/app/api/coupons/apply/route.ts` | T5a: Auth gate; D1 coupon_redemptions check (user + global maxUses) |
| `apps/sophia-ai-factory/src/app/api/errors/report/route.ts` | T5b: Optional auth; anon IP rate limit 10/min; cap message 1024; strip CRLF |
| `apps/sophia-ai-factory/src/app/api/setup/save/route.ts` | T5c: Auth gate |
| `apps/sophia-ai-factory/src/app/api/realtime/alerts/route.ts` | T5d: CRON_SECRET Bearer gate on GET/POST/DELETE |
| `apps/sophia-ai-factory/migrations/0025-coupon-redemptions.sql` | T5a: New migration — coupon_redemptions table |

## Tasks Completed

- [x] T1: CI hard blockers — removed `continue-on-error: true` from Lint and Test steps
- [x] T1: SHA-pinned `actions/checkout` and `actions/setup-node` in both jobs of test.yml
- [x] T2: SHA-pinned both actions in d1-backup.yml
- [x] T2: Added R2 off-site upload step (`continue-on-error: true`; artifact upload remains primary)
- [x] T3: Immutable cache headers for `/_next/static/:path*` and `/static/:path*`
- [x] T3: Existing security headers (HSTS, X-Frame-Options, CSP, etc.) preserved intact
- [x] T4: Removed `https://*.supabase.co` from CSP connect-src
- [x] T4: Added TODO comment for `unsafe-inline` nonce migration (deferred per scope)
- [x] T5a: Auth gate on coupons/apply (401 if unauthenticated)
- [x] T5a: Per-user redemption check in D1 coupon_redemptions (409 if already redeemed)
- [x] T5a: Global maxUses enforcement via COUNT query
- [x] T5a: Migration 0025-coupon-redemptions.sql created
- [x] T5b: getCurrentUser() check on errors/report (anonymous allowed, stricter rate limit)
- [x] T5b: Anon IP rate limit 10 req/min (in-memory, Cloudflare Workers edge reset)
- [x] T5b: message capped at 1024 chars; CRLF stripped from message+stack+url+userAgent
- [x] T5c: Auth gate on setup/save (401 if unauthenticated)
- [x] T5d: CRON_SECRET Bearer gate on all realtime/alerts handlers (GET/POST/DELETE)

## Verification

- Build: **PASS** — `✓ Compiled successfully in 9.3s`
- Tests: **1580 passed / 2 pre-existing failures** (video-creator-wizard.test.tsx — broken by prior commit `bc9ab688`, outside my file ownership, not related to T1-T6)
- Pre-existing failures confirmed: last test file modification was commit `ecd37849` (Phase 2 video wizard), broken in `bc9ab688` which refactored wizard state

## Notes

- R2 bucket `sophia-backups` must be created once: `wrangler r2 bucket create sophia-backups`
- coupon_redemptions migration must be applied to remote D1: `wrangler d1 migrations apply sophia-raas-db --remote`
- The 2 test failures in `video-creator-wizard.test.tsx` are NOT introduced by this phase; they need a separate frontend fix (out of scope)

## Unresolved Questions

- None. All T1-T6 tasks completed within ownership boundary.
