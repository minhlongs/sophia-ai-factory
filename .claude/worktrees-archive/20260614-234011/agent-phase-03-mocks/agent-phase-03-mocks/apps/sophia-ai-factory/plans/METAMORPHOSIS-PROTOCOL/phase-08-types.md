# Phase 8: Types (Type Safety)

**Goal:** robust codebase with static analysis.

## Objectives
1.  **Strict Mode**: Ensure `strict: true`.
2.  **Coverage**: Minimize `any`.
3.  **Accuracy**: Accurate types for external data (Supabase).

## Tasks
- [ ] Run `tsc` and fix all errors.
- [ ] Replace `any` with specific types.
- [ ] Generate types from Supabase schema.
- [ ] Ensure Props interfaces are defined for all components.
