# D-5.1 Next.js Version Bump Report

**Date:** 2026-05-22
**Task:** Close 12 HIGH Dependabot CVEs against `next` (middleware bypass i18n, SSRF WebSocket, DoS Server Components, DoS Cache Components, App Router segment-prefetch)

---

## Before / After

| Package | Before (package.json specifier) | After | Resolved (lockfile) |
|---|---|---|---|
| `next` | `^16.2.3` | `^16.2.5` | `16.2.6` |
| `eslint-config-next` | `16.1.6` (pinned) | `16.2.6` (pinned) | `16.2.6` |
| `@next/bundle-analyzer` | `^16.1.6` | `^16.2.6` | `16.2.6` |
| `@next/eslint-plugin-next` | (transitive) | (transitive) | `16.2.6` ← was `16.1.6` |

The lockfile already had `next@16.2.6` resolved (pnpm had fetched it earlier). Only the `package.json` specifier needed updating to declare `>=16.2.5` as the minimum safe version. `eslint-config-next` and `@next/bundle-analyzer` were lagging at `16.1.6` — bumped to align.

---

## Build

- **Command:** `NODE_OPTIONS=--max-old-space-size=14336 npx next build --turbopack`
- **Result:** `✓ Compiled successfully in 18.0s`
- **Pages:** 181/181 generated (0 errors)
- **Build duration:** ~18s compile + ~127ms static page generation
- **Warnings (pre-existing, not from bump):**
  - `middleware` file convention deprecated → use `proxy` (pre-existing)
  - NFT trace warning on `test-coverage.ts` (pre-existing)
  - `module.register()` DeprecationWarning from Node.js (pre-existing)

---

## Tests

- **Command:** `npx vitest run`
- **Result:** 477 passed | 1 skipped (478 files); 4687 passed | 34 skipped (4721 tests)
- **Duration:** 42.3s
- **Zero regressions from bump**

---

## Dependabot Alert Delta

- **Before:** 61 open alerts against `next` (manifest: `package-lock.json`)
- **After (local):** Specifier now `^16.2.5` → resolves to `16.2.6` which patches all referenced CVEs
- **Note:** Dependabot re-evaluates on next push. Alert count will drop on commit/push (not done per instructions).

**CVEs closed by `next@16.2.5`+:**
- GHSA-vfv6-92ff-j949 (CVE-2026-44582) — cache poisoning via RSC cache-busting collision
- GHSA-* series (middleware bypass i18n, SSRF WebSocket, DoS Server Components, DoS Cache Components, App Router segment-prefetch) — all fixed in `16.2.5` per advisories referencing `>= 16.0.0, < 16.2.5` as vulnerable range

---

## Files Modified

- `apps/sophia-ai-factory/package.json` — 3 version specifier changes
- `apps/sophia-ai-factory/pnpm-lock.yaml` — regenerated (eslint-config-next 16.1.6 → 16.2.6, @next/* 16.1.6 → 16.2.6)
