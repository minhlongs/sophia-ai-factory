# Scout Report — Sophia Mekong Restructure Dependency Graph

> **Status: TEMPLATE — populate during Phase 01 execution.**
> Date: 2026-05-03
> Tool: ts-morph via `scripts/scout-layer-classify.mts`
> Source: `apps/sophia-ai-factory/src/` (1646 TS/TSX files)

---

## 1. Executive Summary

| Layer | File Count | % of Total | Top 3 Dirs |
|-------|-----------:|-----------:|------------|
| seed   | TBD | TBD% | TBD |
| tree   | TBD | TBD% | TBD |
| forest | TBD | TBD% | TBD |
| land   | TBD | TBD% | TBD |
| AMBIGUOUS | TBD | TBD% | (require human review) |
| **Total** | **TBD** | **100%** | — |

Cross-layer violations: TBD upward imports detected.

---

## 2. Per-Layer File Lists

### seed/ (target: src/seed/)
```
<file path 1>
<file path 2>
...
```

### tree/ (target: src/tree/)
```
...
```

### forest/ (target: src/forest/)
```
...
```

### land/ (target: src/land/)
```
...
```

### AMBIGUOUS (require human review BEFORE Phase 03)
```
<path> — could be <layer A> or <layer B> — REASON
```

---

## 3. Cross-Layer Import Violations

### Definition
Layer indexes: seed=0, tree=1, forest=2, land=3.
**Violation:** file at layer X imports from layer Y where X < Y.

### Top 50 Violations (sorted by frequency of offender)

| # | Source File | Source Layer | Imported From | Imported Layer | Violation Type | Suggested Fix |
|---|-------------|:------------:|---------------|:--------------:|----------------|----------------|
| 1 | TBD | TBD | TBD | TBD | TBD | TBD |

### Violations by File (deduplicated)
```
<file>
  imports <upward target 1>
  imports <upward target 2>
  ...
```

---

## 4. Route Files Census (Stay-In-Place List)

Files that MUST remain in `src/app/` per Next.js routing convention:

| Route URL | File Path | Layer (logical) |
|-----------|-----------|-----------------|
| `/` | `src/app/[locale]/page.tsx` | tree (or land if landing-page) |
| `/pricing` | `src/app/[locale]/pricing/page.tsx` | land |
| `/api/status.json` | `src/app/api/status.json/route.ts` | land |
| ... | ... | ... |

(Generated by `find src/app -name 'page.tsx' -o -name 'route.ts' -o -name 'layout.tsx'`)

---

## 5. Dynamic Import / `__dirname` / Template-Literal Hazards

Files using runtime path resolution that codemod CANNOT auto-fix:

```
<file>:<line>  <snippet>
```

These require manual fixes during their respective phase.

---

## 6. Test File Coverage Map

| Test Path Pattern | Source Under Test | Move With |
|-------------------|-------------------|-----------|
| `src/lib/db/__tests__/*` | `src/lib/db/*` | seed (Phase 03) |
| ... | ... | ... |

---

## 7. Pre-Refactor TODOs (block Phase 03 if non-empty)

- [ ] TBD — fix upward import: `<file>` → `<target>`
- [ ] TBD — resolve AMBIGUOUS classification: `<file>` → assign to `<layer>`
- [ ] TBD — handle dynamic import: `<file>:<line>`

---

## 8. Recommended Phase Ordering Adjustments

(Populate if scout reveals dependencies that warrant changing the canonical 03→04→05→06 order.)

---

## Unresolved Questions

(To be appended during execution.)
