# Tasks: {{feature}}
# Generated by `mekong code {{feature}}` — check off as you implement
# Version: 1.0.0 | Phase: code

Feature: {{feature}}
Date: {{YYYY-MM-DD}}
Author: fullstack-developer agent

---

## File Ownership Matrix

| File | Agent | Action | Est. Lines |
|------|-------|--------|-----------|
| `src/{{path}}/{{file}}.py` | fullstack-developer | CREATE | {{N}} |
| `tests/test_{{feature}}.py` | tester | CREATE | {{N}} |
| `src/cli/app_setup.py` | fullstack-developer | MODIFY | +{{N}} |

---

## Task Backlog

### {{Component 1}}
- [ ] [CREATE] {{component1}}: implement {{Class/function}} — accepts {{input}}, returns {{output}}
- [ ] [CREATE] {{component1}}: implement {{function2}} — {{acceptance criterion}}
- [ ] [TEST] {{component1}}: test {{behaviour}} — assert {{expected result}}
- [ ] [TEST] {{component1}}: test error path — assert {{error raised/returned}}

### {{Component 2}}
- [ ] [CREATE] {{component2}}: implement {{function}} — {{acceptance criterion}}
- [ ] [TEST] {{component2}}: test {{behaviour}} — assert {{expected result}}

### CLI Integration
- [ ] [MODIFY] app_setup.py: import and register `{{command_name}}` sub-app
- [ ] [TEST] cli: test `mekong {{command_name}} --help` exits 0

---

## Test Coverage Plan

```
tests/test_{{feature}}.py — covers:
  - {{behaviour 1}}
  - {{behaviour 2}}
  - error path: {{error scenario}}
```

---

## Integration Checklist

- [ ] Register command in `src/cli/app_setup.py`
- [ ] Instrument with `METERS.invocation_ms` if crosses agent boundary
  (from `src/core/telemetry/meters.py`)
- [ ] Instrument with `METERS.token_cost_usd` if LLM is called
- [ ] Call `emit_mission_event()` if feature runs a mission
  (from `src/core/signals/local_store.py`)
- [ ] Verify `mekong eval-agent {{feature}}` returns data after first run
- [ ] Verify `mekong metrics` shows updated counts

---

## Complexity Estimates

| Task | Size | Notes |
|------|------|-------|
| {{task 1}} | S | <30 min |
| {{task 2}} | M | <2 h |
| {{task 3}} | L | <1 day — consider splitting |

---

## Blocked By

- {{task N}} must complete before {{task M}}
- {{dependency on another feature or PR}}
