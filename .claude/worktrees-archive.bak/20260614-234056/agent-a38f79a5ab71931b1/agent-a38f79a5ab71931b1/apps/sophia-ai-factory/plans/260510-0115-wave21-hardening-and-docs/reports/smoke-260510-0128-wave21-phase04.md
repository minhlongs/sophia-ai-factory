---
title: "Wave 21 Phase 04 — Production Smoke + Inngest Retry Verification"
type: smoke-report
date: 2026-05-10T01:28 PT
production_url: https://sophia.agencyos.network
deployed_sha: 9f051edd (Wave 20 Phase 02)
local_sha_at_test: cab5925f (Wave 21 Phase 03 — docs-only, no deploy needed)
---

# Wave 21 Phase 04 — Smoke Report

## 1. Deploy state

```
Live shortSha:    9f051edd  (Wave 20 P02 — Telegram step split)
Live deployedAt:  2026-05-10T08:08:27Z
opennextVersion:  1.17.3
Local HEAD:       cab5925f  (Wave 21 P03 — docs-only)
```

**Verdict:** Live SHA cố ý không match local — Phase 03 + Phase 04 docs-only commits, không cần deploy. Production còn ở trạng thái Wave 20 cuối cùng.

## 2. HTTP smoke (unauthenticated)

| Path | Code | Expected | Status |
|---|---|---|---|
| `/` | 200 | 200 | ✅ |
| `/api/version` | 200 | 200 | ✅ |
| `/api/health` | 200 | 200 | ✅ |
| `/api/quota/status` | 401 | 401 (auth wall) | ✅ |
| `/api/billing/usage-summary` | 401 | 401 | ✅ |
| `/api/account/change-email` | 405 | 405 (POST-only) | ✅ |
| `/api/account` | 405 | 405 (DELETE-only) | ✅ |
| `/en/dashboard` | 308 | 3xx (auth redirect) | ✅ |
| `/vi/dashboard` | 307 | 3xx (auth redirect) | ✅ |

**Headers:** HTTP/2, hreflang en+vi correctly emitted, `cache-control: public, s-maxage=60, stale-while-revalidate=600`.

**Verdict:** ✅ ALL ENDPOINTS HEALTHY.

## 3. /api/health body

```json
{"status":"healthy","timestamp":"2026-05-10T08:28:57.529Z"}
```

## 4. Inngest retry classification — local test verification

Phase 02 Telegram step split không thể end-to-end test trên production qua curl (cần authenticated user + Telegram bot). Verified via local test suites:

```
src/tree/telegram/__tests__/dispatch-with-retry-hints.test.ts
  → 14 tests pass — RetryAfterError throw on 429 with retryAfterSec
src/forest/inngest/functions/__tests__/publish-execute-video-url-wave17.test.ts
  → 9 tests pass — claim-and-upload + telegram-send + telegram-finalize step memoization
src/forest/components/dashboard/__tests__/sidebar-quota-widget.test.tsx
  → 9 tests pass — widget loading/MASTER/threshold colors
src/app/api/account/change-email/__tests__/
  → 12 tests pass — POST + verify GET handlers
```

**Total Wave 20 test coverage:** 44 tests pass on commit `cab5925f`.

## 5. Inngest production retry — MANUAL VERIFICATION REQUIRED

Phase 02 fix's real-world correctness (CAS không replay khi telegram 429 retry, `RetryAfterError` honored) cần manual smoke trong production:

**Procedure (manual, CEO/operator):**
1. Đăng nhập sophia.agencyos.network với FREE100 account
2. Trigger publish job tới Telegram channel (qua Distribute UI)
3. Quan sát Inngest dashboard: nếu Telegram 429 xảy ra (rate-limit), confirm:
   - Function NOT replay `claim-and-upload` step (no double CAS)
   - `telegram-send` step memoize đúng output
   - Wait time honored = `retry_after` từ Telegram error
4. Confirm publishing_jobs.status không bị stuck `processing` indefinitely

**KHÔNG** auto-test được vì cần real Telegram API + authenticated session.

## 6. Findings

KHÔNG phát hiện regression mới ở production. Tất cả smoke pass.

5 findings từ Phase 03 code-review vẫn remain WAVE 22 BACKLOG (không actionable trong Phase 04).

## Unresolved

1. **CEO smoke test #234** — vẫn pending, cần manual verify Distribute → Telegram flow trên production.
2. **Inngest retry integration test** — không có cheap auto-test path; chấp nhận manual smoke là gate duy nhất.
3. Wave 22 Q: Có nên invest E2E Playwright test với mocked Telegram API server để cover retry behavior? (effort vs value đánh giá ở Wave 22 plan).
